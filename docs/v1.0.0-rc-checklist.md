# v1.0.0-rc Checklist

Self-review checklist for Patrick Guest before cutting the `v1.0.0-rc` tag.

### Core functionality
- [x] All six built-in profiles evaluate correctly and pass their snapshot tests
- [x] `atb verify --format json` output matches `VerifierReport` struct definition in `internal/verify/report.go`
- [x] `atb trust-report --format json` output matches `TrustReport` struct definition in `internal/verify/trust_report.go`
- [x] `atb export --format compliance --json` output matches `ComplianceManifest` struct in `internal/export/manifest.go`
- [x] Chain integrity failure sets `ResidualRisk` to `Critical`
- [x] `--dry-run` on verify exits 0 with `informational_notes: ["dry-run: no evaluation performed"]`
- [x] Auto-detection resolves correct profile for `rag_answer`, `privileged_tool_action`, and `background_automation` bundles

### Test coverage
- [ ] `go test ./...` passes cleanly on ubuntu, macos, and windows
- [x] `TestTrustReportJSONSnapshot`: all six profiles pass including negative cases
- [x] `TestVerifyJSONSnapshot`: all six profiles pass including chain tamper detection
- [x] `TestExportComplianceManifestGolden`: golden file matches
- [x] SDK event type constant tests pass (Python and TypeScript)
- [x] `BenchmarkBundleAppend100` and `BenchmarkVerifyChain100` produce results (no regression check, existence only)

### Documentation
- [x] `docs/spec-v1.0.md` reflects current profile templates (verified post-0.9.2 alignment)
- [x] `docs/profiles.md` includes CAS support column and correct required events for all six profiles
- [x] `docs/ciso-acceptance-guide.md` references correct CAS sub-score keys (`EC`, `FC`, `RC`, `TC`, `SC`, `XC`, `AC`, `GC`)
- [x] `docs/key-management.md` includes complete key rotation section
- [x] `docs/integrations/mcp.md` committed and accurate
- [x] `docs/performance.md` committed with M2 baseline numbers
- [ ] `AGENTS.md` reflects current repo layout

### Security and compliance
- [x] `go vet ./...` passes with no warnings
- [ ] Security Gate (Layer D) CI passes: Go, Node, Python scans
- [ ] No secrets present in tracked files (run `atb secret-scan` or equivalent)
- [x] Ed25519 signing path tested in at least one integration test
- [x] TSA anchoring path documented in `ciso-acceptance-guide.md`

### Release mechanics
- [x] Version constant in `cmd/atb/main.go` matches target release
- [x] Python SDK version matches (PEP 440 form)
- [x] TypeScript SDK version matches
- [x] `web/package.json` version matches
- [x] `CHANGELOG.md` has entry for target version in correct heading format (`## [vX.Y.Z]`)
- [ ] CI green on ubuntu, macos, and windows for HEAD
- [ ] Docs sync smoke check passes
- [x] `v1.0.0-rc` tag does not yet exist (confirm before tagging)

### Known limitations to document before rc
- [x] Native MCP server mode not yet implemented; CLI wrapping required
- [x] CAS scoring not available for `data_export`, `background_automation`, `policy_decision`, or `human_override` profiles
- [x] `atb view --ui-experimental` is not a stable API surface
- [ ] Windows path handling in bundle storage: confirmed working in CI but not independently benchmarked

### How to use this checklist
Each item must be manually confirmed or confirmed by CI before cutting the `v1.0.0-rc` tag. Items marked `- [x]` are confirmed. Items left as `- [ ]` are open. Do not cut the tag until all items in `Core functionality`, `Test coverage`, and `Release mechanics` are checked. `Known limitations` items are documentation tasks, not blockers.
