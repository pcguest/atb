# Web testing roadmap

These items are planned for v1.2+ and are tracked here rather than in the web README to keep the README release-accurate.

- Replace mock API with real `atb view --ui-experimental` server in E2E
- Integrate Puppeteer for Lighthouse in CI
- Add visual regression tests (Percy/Chromatic)
