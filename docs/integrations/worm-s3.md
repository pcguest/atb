# WORM/S3 export

`atb push` uploads a bundle to an S3 or S3-compatible endpoint and can request object-lock retention when configured.
For normative behaviour, trust boundaries, limits, and CLI contract, see [`docs/spec/bundle-push.md`](../spec/bundle-push.md).

