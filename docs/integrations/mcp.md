# MCP Integration Notes

ATB currently does not ship a first-party MCP server.

Use this page to document:
- how MCP tools should emit canonical ATB events (`ai.*` plus bundle lifecycle events),
- minimum event sets for profile evaluation, and
- verification expectations (`atb verify`, `atb trust-report`) for MCP-driven workflows.
