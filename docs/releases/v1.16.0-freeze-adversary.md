# ATB v1.16 freeze — Subagent J hostile adversary

**Role:** read-only freeze reviewer. Assume A–I are too optimistic. Try to FAIL.  
**Product edits:** none. No version bump. No push. No merge. No tag. No feature ideas.  
**Candidate SHA:** `a607f244012bb8ca0faf81209cace38f73e8093d` (`a607f24`)  
**Branch:** `feat/v1.16.0-semantics`  
**Baseline:** `v1.15.4^{}` = `53ad440cc872a685a1e1580e99b74b7e3c150a63`  
**Safety ref:** `safety/pre-v116-main-recovery-20260902T1439` → `9afbd0ac9a6b8bae0bdf7c04c80cea9bddbaf0d3`

**Verdict: PASS WITH DOCUMENTED DEBT**

Attempted FAILs that did **not** meet the freeze FAIL bar are recorded below. No FAIL IDs. Documented debt is listed, not invented as new product work.

---

## Independent git identity (spot-check, live)

| Pointer | Observed | Expected | Result |
| --- | --- | --- | --- |
| `HEAD` | `a607f244012bb8ca0faf81209cace38f73e8093d` | freeze candidate | **match** |
| branch | `feat/v1.16.0-semantics` | same | **match** |
| local `main` | `53ad440cc872a685a1e1580e99b74b7e3c150a63` | peeled `v1.15.4` | **match** |
| `origin/main` | `53ad440cc872a685a1e1580e99b74b7e3c150a63` | same | **match** |
| `git rev-parse v1.15.4` (tag object) | `6843429c60dfbc8517f7be4e852a98614727d833` | annotated tag | **match ledger** |
| `v1.15.4^{commit}` | `53ad440cc872a685a1e1580e99b74b7e3c150a63` | baseline | **match** |
| `git merge-base HEAD main` | `53ad440cc872a685a1e1580e99b74b7e3c150a63` | baseline | **match** |
| safety ref | `9afbd0ac9a6b8bae0bdf7c04c80cea9bddbaf0d3` | still present | **match** |
| `v1.16*` tags | none | none until authorised | **match** |
| `git describe --tags --always --dirty` | `v1.15.4-16-ga607f24` | not `v1.16.0`; 16 commits after tag (park + honesty merge + semantics + gold/view/a11y/claims) | **coherent** |
| product version | `cmd/atb/main.go` `1.15.4`; web/TS/Python `1.15.4`; custody `verify.report.v1.schema.3` | not bumped | **coherent** |
| tracked dirt | none | clean | **match** |
| untracked | `V116_*.md` certification artefacts only (plus this report after write) | allowed | **match** |

Ancestry: `53ad440`, `f9c4d6e`, and `ea345fa` are all ancestors of `HEAD`. Working tree is not a dirty product tree.

Checkpoint A’s candidate-state file still names `f9c4d6e`. That is historical Checkpoint A, not the freeze tip. Ledger candidate is `a607f24`. Not a FAIL.

---

## SHA discipline (the question that could FAIL freeze)

| Proof | Certified SHA | After F-CLAIMS-01 (`ea345fa..a607f24`) |
| --- | --- | --- |
| Semantic (Subagent C) | `f9c4d6eb029d81fc689e4c7597a85793d165bc72` | `git diff f9c4d6e..HEAD -- internal/mcp internal/verify pkg/custody pkg/otel internal/capabilities internal/contextlineage schemas` is **empty** |
| Live View / Firefox / a11y / performance | `ea345fad651f7585693e35c4adc983433870e2de` | `git diff ea345fa..HEAD -- web/app/view pkg/api/v1 cmd/atb/view.go internal/mortise` is **empty** |
| Mortise | `ea345fa` | same empty Mortise/view/API diff |
| Gold + claims + consumer | `a607f24` | recertified on this SHA |

`git log --name-status ea345fa..a607f24` is only:

```
a607f24 docs: stop overclaiming reveal sidecar and Trust Dashboard
M	docs/specification/viewer.md
M	web/components/Features.tsx
```

`Features.tsx` is imported only by marketing `web/app/page.tsx`, not by `web/app/view/page.tsx`. Gold correctly recertified because Features ships in the Next.js `output: "export"` webpack build. Investigation `/view` source is byte-identical to `ea345fa`. Consumer proof re-walked View on `a607f24`. Gold live E2E (`live-dashboard.cy.ts` 2/2) and `test:a11y` also ran on `a607f24`. Log `/tmp/atb-gold-a607f24.log` ends `✅ All gold release gates passed`.

**Carry without full C/Mortise/semantic rerun is not a freeze FAIL.** Docs-only F-CLAIMS-01 does not change custody, MCP advertising, View investigation behaviour, or the ATB→Mortise ingest artefact. Gold + claims + consumer on the tip are the required recertifications. Semantic and Mortise trees are unchanged after their proof SHAs.

Attempted FAIL **F-ADV-SHA-01** (carry invalid): **rejected**.

---

## Attempted FAILs (did not land)

Each row was treated as a freeze FAIL until disproved.

### F-ADV-SHA-01 — F-CLAIMS-01 carry invalidates C / Mortise / semantic

**Rejected.** Evidence above. Investigation embed at `a607f24` is a new webpack export of unchanged `/view` source plus landing copy. That invalidates gold (recertified) and claims (recertified), not Mortise hashing or CAS omit.

### F-ADV-CAS-01 — invalid integrity still exposes trusted derived coverage

**Rejected as FAIL.** Custody `VerifierReport` omitempty + note `coverage_score omitted because integrity_valid is false` (C case 2; Mortise tamper copy; D Trust JSON; G tamper View). API `cloneProfileSummary` zeros coverage when `!integrityValid`; `handleInvestigationTrust` copies coverage only if both integrity flags are true. View `evidenceCoverageLabel` returns `Untrusted` before any percent; TrustSurface uses `!trust.integrity_valid → "Untrusted"` then `coverage_grade`, never a raw zod `0`. Tamper wall blocks event-derived surfaces (`403` / empty-state). Live D: no `%`, no Moderate coverage on tampered RAG-pass.

**Latent (debt, not FAIL):** `web/lib/schemas/investigation.ts` and `profile.ts` still `coverage_score: z.number().optional().default(0)`. Parsed client objects can contain `0` when the wire omitted the key. Shipped Trust/Incident do not render that `0` as assessed coverage. Do not later bind a percent to the parsed number without `coverage_grade` / `integrity_valid`.

### F-ADV-CTX-01 — Context still overclaims “supplied to model”

**Rejected.** Heading is `Context evidence`. Empty copy: “does not prove retrieval did not occur.” Split copy when capabilities exist without units: “Retrieval was recorded; structured lineage units were not.” “Supplied to model” appears only as a **limit** (“only applies when an invocation bind is proven”). Live D TB-UI-01 PASS. Vitest asserts the heading is not “Context supplied to model”.

### F-ADV-MCP-01 — MCP still implies 2026-07-28 completeness

**Rejected.** `handleInitialize` / `handleDiscover` advertise only `LegacyProtocolVersion` (`2024-11-05`). Constant `ProtocolVersion = "2026-07-28"` and unused alias `protocolVersion = ProtocolVersion` exist as a target; they are not copied into initialize/discover JSON. Docs: do not claim 2026-07-28 completeness. C case 11 PASS. Dead constant is not an advertised protocol.

### F-ADV-PAGE-01 — PageIndex disappears despite recorded retrieval

**Rejected on proven path.** Overlay still badges `PageIndex adapter` when `raw_event_type` starts with `atb.event.rag_`. Live D TB-UI-02 on the PageIndex fixture showed the split sentence + adapter. C cases 4/6: type stays `atb.event.rag_retrieval`, not rewritten to `ai.retrieval.executed`. If capabilities were empty while a rag_retrieval event existed, Context would under-claim; that composition was live-proven, not missing.

### F-ADV-EVID-01 — Findings / Timeline cannot resolve to exact Evidence

**Rejected as freeze FAIL; recorded as documented debt (F-ADV-DEBT-01).**

Live D: Findings `#2` → Evidence `atb.tool.call` PASS; Timeline row → Evidence PASS on incident-demo / PTA (seqs ≪ 200). Vitest `opens evidence from a finding sequence` / `opens evidence from a timeline row`.

Independent code: Evidence is paginated at 200 (`useBundleEventsQuery`). `openEvidence` only `setSelectedSeq` + `setSurface("evidence")`. Selection is:

```
events.find((event) => event.seq === selectedSeq) ?? events[0] ?? null
```

If the supporting seq is **not** already in loaded pages, the inspector shows **another** loaded record (typically seq on page 1) until the user happens to Load more. Timeline lists all events; Evidence does not. That is false certainty on large bundles, not on the freeze’s PTA/incident-demo proofs.

This is the same Evidence pagination architecture Checkpoint C already accepted as performance debt. It does not break the proven small-bundle investigation contract. It is **not** a P0 integrity leak (tamper still 403s event payloads). Promoting it to FAIL would be a product change (do not substitute `events[0]`; optionally seek the page). Freeze FAIL bar is material unusable / contract contradiction on shipped realistic fixtures — not met.

### F-ADV-HEALTH-01 — legacy Health / Trust Dashboard reachable from shipped View

**Rejected.** `TrustScoreCard`, `ExecutiveSummaryPanel`, `AuditorCompliancePanel`, `ProfileCAS`, `SessionAnomalies` are not imported by `web/app/view/page.tsx`. View mounts `EventInspector` + optional `TraceGraph` only from `components/dashboard`. `atb view` mux serves `/view/` + `/_next/` and redirects `/` (and unmatched paths including `/sessions/`) to `/view/`. Session HTML exists in the Next export but is not a served `atb view` route. Live D: no Viewer Health. Navbar `/sessions` link lives on marketing `Navbar`, not investigation View.

Dead Health components remaining in tree: **accepted dated debt** (already in ledger).

### F-ADV-VIEW-01 — View parity materially fails (export)

**Rejected.** No View export control; `AuditorCompliancePanel` unmounted. CLI `atb export` / `atb incident export` proven in G. Public docs describe CLI export and do not claim a View button. Same class as freeze-accepted dated debt. **Not** a material View-parity FAIL.

### F-ADV-TEST-01 — gold still asserts retired Health implementation

**Rejected as FAIL.** F-GOLD-01 smoke now asserts `view-shell` / “Verifying bundle integrity”, with an explicit comment not to wait for Trust Dashboard copy. Gold E2E body checks Integrity / Coverage / Corroboration. Suite **title** `describe("Trust Dashboard against the embedded ATB server")` and leftover `dashboard.cy.ts` / `trust-score.cy.ts` (Viewer Health `100/100`) are **not** `make gate-gold-release` specs (`test-e2e` → `live-dashboard.cy.ts` only). `web/package.json` `test:e2e` still points at `dashboard.cy.ts` — STALE harness, not a gold PASS built on Health HTML.

### F-ADV-GEN-01 — generated files inconsistent / versions incoherent

**Rejected as FAIL.** Gold `check-generated` and `check-versions` PASSed on `a607f24` (`ok: all version strings agree (1.15.4)`). Custody pin `verify.report.v1.schema.3`; schema `$id` schema.3. No `v1.16.0` product marker. Quickstart still says PyPI/npm `1.14.5` vs source `v1.15.2` while HEAD is `1.15.4` — **accepted claims STALE**, not a schema.3 / CLI mismatch.

### F-ADV-MORT-01 — Mortise still pinned to ATB v1.14.3

**Rejected as ATB freeze FAIL.** F-MORTISE-01 is Mortise-repo lag (`pcguest/mortise` `main@22f1d05` `require github.com/pcguest/atb v1.14.3`). Schema.3 is additive; ATB ingest bytes and `custody.Evaluate` hold on `ea345fa`. ATB freeze ≠ Mortise freeze. **Dated compatibility debt.**

### F-ADV-SCOPE-01 — corrective commits out of freeze scope

**Rejected.** F-GOLD-01..05 are gold-gate blockers (stale smoke, Trivy, gosec XSS encode, browserslist, contrast). F-VIEW-01/02 are P0 investigation correctness (omitted coverage rendered 0%; reveal bind). F-A11Y-01 is the Firefox focus P0. F-CLAIMS-01 is public overclaim copy. Accidental CommandPalette files in `29cda1a` were reverted (`11a7b05`); net zero. F-GOLD-03 changes investigation report HTML escaping only. None of these is GROUP 7, DSL v2, market P1/P2, or a new surface.

### F-ADV-ENV-01 — consumer Node 25 / Python 3.14 vs support matrix

**Rejected as freeze FAIL.** Gold used Node **v22.23.1** (pin). Gold Python parity uses `.venv` 3.11. G stranger journey also used Homebrew Node **25.9.0** and Python **3.14.6** and recorded that as ENVIRONMENT, not PASS-because-newer. Build still succeeded; matrix is Node 22 / Python 3.9–3.12. **Documented environment assumption**, not a product defect. Do not read G as proof that 3.14/25 are supported.

---

## Direct answers

| Question | Answer |
| --- | --- |
| Does any PASS depend on local environment assumptions? | **Yes, documented.** Gold: pinned Go 1.26.7, Node 22, Firefox 154.0.1, `.tmp/bin` scanners. C/E/F: cursor-ide-browser MCP did not attach; Cypress Electron/Firefox substituted. G: Node 25 / Python 3.14 / Selenium / Colima `/tmp` bind-mount. Mortise: no live `mortise-ingestd`; GitHub pin inspect. None of these recasts a product FAIL as PASS. |
| Does any UI state communicate more certainty than evidence supports? | **Bounded.** Trust three questions and Context empty-state are honest on proven fixtures. **Debt:** Evidence inspector `?? events[0]` if selected seq is not on a loaded page (F-ADV-DEBT-01). Zod `default(0)` on omitted `coverage_score` is latent; shipped Trust/Incident do not display it as assessed coverage when integrity failed or grade is absent. |
| Does invalid integrity expose trusted derived coverage anywhere? | **Not on custody JSON, investigation Trust/overview, or shipped View percents.** Client zod may materialise `0` internally. ProfileCAS `coverage_score ?? cas_score` exists but is unmounted. |
| Does Context still overclaim anywhere? | **Not on the heading or empty-state.** RoleSelector `aria-label="Select dashboard role"` remains STALE presentation (accepted). `investigationSummary` API empty-findings sentence remains weaker than View copy (TB-UI-03, accepted). |
| Does MCP still imply unsupported modern protocol support? | **No on the wire or public MCP doc.** Target constant `2026-07-28` remains unpublished completeness. |
| Does PageIndex disappear from investigation despite recorded retrieval? | **No on the live-proven adapter fixture.** Overlay still labels PageIndex when `atb.event.rag_*`. |
| Can Findings resolve to exact Evidence? | **Yes on PTA / incident-demo (live D).** Not guaranteed for seq outside loaded Evidence pages (debt). |
| Can Timeline resolve to exact Evidence? | **Yes on those same small fixtures.** Same pagination bound as Findings. |
| Are legacy Health surfaces accidentally reachable? | **Not from `atb view` investigation chrome or mux routes.** Dead components remain in tree (accepted debt). |
| Does View parity materially fail? | **No.** Export CLI-only is dated debt. Coverage/reveal P0s were fixed and live-proven on `ea345fa`, then gold/consumer recertified on `a607f24`. |
| Did any test assert implementation rather than contract? | **Gold path no longer does** (F-GOLD-01). Leftover `dashboard.cy.ts` / `trust-score.cy.ts` still assert Viewer Health; they are not gold. Cypress describe titles still say “Trust Dashboard” (STALE). |
| Are generated files consistent? | **Yes per gold `check-generated` on `a607f24`.** Not independently regenerated this pass. |
| Are version/schema references coherent? | **Product/SDK/CLI/OpenAPI = 1.15.4; custody schema.3.** Quickstart registry/source versions are STALE (`1.14.5` / `v1.15.2`). No `v1.16.0` tag. |
| Is docs-only F-CLAIMS-01 enough to carry C / Mortise / semantic? | **Yes.** Not a freeze FAIL. See SHA discipline. |
| Corrective commits out of freeze scope? | **No.** |
| Export CLI-only — material View parity fail? | **No. Dated debt.** |
| F-MORTISE-01 pin v1.14.3 — freeze fail or dated debt? | **Dated compatibility debt.** |
| Consumer Node 25 / Python 3.14 vs matrix? | **Environment assumption. Documented. Not FAIL.** |

---

## Documented debt (non-blocking)

Carry forward from ledger, plus adversary-only footnotes. No new product work.

1. **F-CAS-02** — temporal consistency when timestamps are missing.  
2. **Legacy Health / TrustScore / ProfileCAS / SessionAnomalies** — remain in tree; unreachable from shipped `/view`.  
3. **Market P1/P2** — URL `?surface=&seq=`, provenance badges, richer type filters.  
4. **GROUP 7** — `cli.py` / `--custos` removal.  
5. **View export** — CLI-only; `AuditorCompliancePanel` unmounted.  
6. **Checkpoint C performance** — Timeline not virtualised; timeline/context/relationships unpaginated and prefetched; graph unbounded; do not advertise 10k smoothness.  
7. **F-MORTISE-01** — Mortise `main` still `github.com/pcguest/atb v1.14.3` vs this freeze schema.3.  
8. **G7 Python PageIndex extra** — still stores plaintext `query`; MCP digest-default is the F-MCP-02 contract.  
9. **Claims STALE (accepted)** — quickstart `1.14.5`/`v1.15.2`; `web/README.md` Trust Score; `viewer-contract.md` Trust Dashboard shell; Features Local Viewer timeline/graph/inspector (incomplete vs Incident-first); workspace “immutable on disk”; RoleSelector “dashboard role”; Cypress suite titles.  
10. **F-ADV-DEBT-01** — Evidence inspector falls back to `events[0]` when `selectedSeq` is not in loaded pages (Findings/Timeline → Evidence on bundles with seq > current Evidence page). Proven exact on PTA/incident-demo only.  
11. **F-ADV-DEBT-02** — G consumer SDK/Docker walk used Node 25.9.0 and Python 3.14.6; support matrix is Node 22 / Python 3.9–3.12. Gold Node 22 remains the freeze web pin.  
12. **F-ADV-DEBT-03** — `web/package.json` `test:e2e` still runs `dashboard.cy.ts` (Viewer Health). Gold does not.  
13. **F-ADV-DEBT-04** — Client zod `coverage_score` `default(0)` on omitted keys; UI currently gated; do not regress.  
14. **F-ADV-DEBT-05** — `viewer.md` still present-tense ships `/sessions` SessionList; `atb view` mux redirects `/sessions/` to `/view/` (API exists; that HTML route is not what `atb view` opens). Claims treated as compatibility/remaining spec, not B2.  
15. **Checkpoint E overall** — claims PASS on `a607f24` + Mortise PASS WITH DATED COMPATIBILITY DEBT on `ea345fa`. E is not a clean PASS; that is why freeze is not a clean PASS.

---

## Corrective commits (scope check)

| ID | SHA | In freeze scope? |
| --- | --- | --- |
| F-GOLD-01 | `f71d4d4` | yes — gold smoke / Cypress helpers |
| F-GOLD-02 | `d8920d4` | yes — Trivy HIGH/CRITICAL pins |
| F-GOLD-03 | `223c30b` | yes — gosec G705 encode; not a new surface |
| F-GOLD-04 | `813da07` | yes — npm audit HIGH browserslist |
| F-GOLD-05 | `34b87a1` | yes — live axe contrast token |
| F-VIEW-01 | `1208130` | yes — P0 omitted coverage as 0% |
| F-VIEW-02 | `9c6bbcc` (after revert `11a7b05`) | yes — P0 reveal bind |
| F-A11Y-01 | `ea345fa` | yes — Firefox Escape focus |
| F-CLAIMS-01 | `a607f24` | yes — public copy vs shipped behaviour |

---

## Checkpoint roll-up (adversary)

| Checkpoint | Status this freeze will carry |
| --- | --- |
| A identity | PASS (after authorised main recovery) |
| B gold | PASS on `a607f24` (independent log tail confirms) |
| B semantic | PASS on `f9c4d6e`; contracts unchanged through `HEAD` |
| C live / Firefox / a11y | PASS on `ea345fa`; not invalidated by F-CLAIMS-01 |
| C performance | PASS WITH DOCUMENTED DEBT on `ea345fa` |
| D consumer | PASS on `a607f24` with ENVIRONMENT notes |
| E claims | PASS on `a607f24` |
| E Mortise | PASS WITH DATED COMPATIBILITY DEBT on `ea345fa` |
| **F adversary** | **PASS WITH DOCUMENTED DEBT** on `a607f24` |

Release-prep / hosted CI / PR / merge / publication: not authorised. Not reviewed as done.

---

## Verdict

**PASS WITH DOCUMENTED DEBT**

No freeze FAIL IDs. The candidate may proceed only with the debt list above unchanged; do not advertise 10k smoothness, View export, Mortise re-pin, Node 25 / Python 3.14 support, or Findings/Timeline → Evidence for seqs not yet loaded in Evidence pages.

Candidate SHA: `a607f244012bb8ca0faf81209cace38f73e8093d`  
FAIL ids: _(none)_  
Report path: `/Users/paddyguest/atb/ATB_V116_FREEZE_ADVERSARY.md`
