# ATB v1.16 Gold-Gate Report

> Historical evidence for the candidate SHA(s) named below. This document does
> not certify the current candidate or authorise further implementation/release
> steps. Current status: [local product convergence](./v1.16.0-product-convergence.md).
> Original scratch filenames and local paths are preserved as historical provenance.

**Certified SHA for this run:** `a607f244012bb8ca0faf81209cace38f73e8093d`  
This PASS/FAIL belongs to that SHA only. It is not a “latest passed” claim.

**Verdict: PASS**

**Prior SHAs:** semantic PASS `f9c4d6e`; F-GOLD-01 gold FAIL smoke `f71d4d4`; F-GOLD-02 gold FAIL gosec `d8920d4`; F-GOLD-03 gold FAIL web npm audit HIGH `223c30b`; F-GOLD-04 gold FAIL live a11y `color-contrast` `813da07`; F-GOLD-05 gold PASS `34b87a1` (INVALIDATED — product P0s landed after it); F-A11Y-01 gold PASS `ea345fa` (**INVALIDATED** — F-CLAIMS-01 copy landed in the gold Next.js export). This SHA is F-CLAIMS-01 on F-A11Y-01.

**Log ancestry (confirmed in `/tmp/atb-gold-a607f24.log` and `git log`):**

- F-VIEW-01 coverage `120813091d477494b32b74acbbbec8732ad298b9` (`1208130`)
- F-VIEW-02 reveal `9c6bbccabac103b76e7b3454dcff23a5a225026f` (`9c6bbcc`)
- F-A11Y-01 palette focus `ea345fad651f7585693e35c4adc983433870e2de` (`ea345fa`)
- F-CLAIMS-01 copy `a607f244012bb8ca0faf81209cace38f73e8093d` (`a607f24`) — `web/components/Features.tsx` sidecar (reveal appends to `<bundle>.reveals`, bundle not mutated); `docs/specification/viewer.md` Trust Dashboard / Health chrome removed from shipped `/view`

**Branch:** `feat/v1.16.0-semantics`  
**Verified at inspection:** `git rev-parse HEAD` = `a607f244012bb8ca0faf81209cace38f73e8093d`. F-CLAIMS-01 HEAD: `docs: stop overclaiming reveal sidecar and Trust Dashboard`. HEAD did not move further.

**Aggregate command:**

```bash
cd /Users/paddyguest/atb
env -u ELECTRON_RUN_AS_NODE PATH="/Applications/Firefox.app/Contents/MacOS:$PWD/.tmp/bin:$PATH" GOTOOLCHAIN=go1.26.7 make gate-gold-release
```

**Exit code:** 0  
**Started:** 2026-09-02T08:28:38Z  
**Ended:** 2026-09-02T08:32:32Z  
**Log:** `/tmp/atb-gold-a607f24.log` (not committed)

How this run was obtained: **fresh authorised run**. Confirmed no duplicate `make gate-gold-release` for this SHA before start. Wrapper PID 11144.

No product code was edited during this certification. Thresholds were not weakened. Nothing was pushed or version-bumped.

---

## Recorded versions vs pins

| Tool | Pin | Recorded | Satisfied |
| --- | --- | --- | --- |
| Go | 1.26.7 | `GOVERSION=go1.26.7` (preflight + log header) | **yes** |
| Node | 22 | v22.23.1 | **yes** |
| staticcheck | v0.7.0 | `.tmp/bin` `staticcheck 2026.1 (v0.7.0)` | **yes** |
| govulncheck | v1.5.0 | `.tmp/bin` `govulncheck@v1.5.0`; `no vulnerabilities reported` | **yes** |
| gosec | v2.27.1 | log Summary `Gosec : 2.27.1`; Issues 0 | **yes** |
| Trivy | 0.73.0 | log `current version is 0.73.0`; `trivy-report.json` HIGH/CRITICAL **0** | **yes** |
| Firefox | Cypress `--browser firefox` | `/Applications/Firefox.app/Contents/MacOS/firefox`; Mozilla Firefox **154.0.1**; Cypress `Browser: Firefox 154 (headless)` | **yes** |

**Go pin satisfied: yes.**

---

## Sub-gates

| Sub-gate | Result | Evidence |
| --- | --- | --- |
| `make build` | **PASS** | `✅ Built ./atb with embedded viewer` |
| `check-generated` / versions / notices / gofmt / vet / staticcheck | **PASS** | `ok: all version strings agree (1.15.4)`; hygiene-quick continued |
| `test-go` | **PASS** | including `cmd/atb`, `test/golden`, `test/schema`, `test/release` |
| viewer lint + typecheck | **PASS** | eslint `--max-warnings=0`, `tsc --noEmit` |
| race | **PASS** | `go test ./... -race` |
| coverage | **PASS** | **80.3%** (`✅ Total Go coverage 80.3%`, ≥80%) |
| hygiene-full benches + `test-all` | **PASS** | `✅ Hygiene gate passed` / `✅ All tests passed` |
| Trivy fs HIGH/CRITICAL | **PASS** | 0 HIGH/CRITICAL in `trivy-report.json` (F-GOLD-02 still holds) |
| gosec (all severities; Makefile does not pass `-severity high`) | **PASS** | Files 168, Lines 36762, Nosec 64, **Issues 0** |
| govulncheck | **PASS** | `govulncheck: no vulnerabilities reported` |
| `cd web && npm audit --audit-level=high` | **PASS** | 1 **low** only (`postcss-selector-parser@6.1.2` GHSA-w9m9-85wc-3x92). browserslist HIGH **cleared** (F-GOLD-04) |
| `cd sdk/typescript && npm audit --audit-level=high` | **PASS** | `found 0 vulnerabilities` |
| `make coverage-check` (post-security recheck) | **PASS** | `✅ Total Go coverage 80.3%` |
| `make test-e2e` | **PASS** | Cypress Firefox `live-dashboard.cy.ts`: **2 passing**, 0 failing |
| `npm run test:a11y` | **PASS** | Cypress Firefox `a11y.cy.ts`: **1 passing**, 0 failing |

---

## Live E2E

`make test-e2e` → Cypress `web/cypress/e2e/live-dashboard.cy.ts` against live `atb view` on `http://127.0.0.1:18888`, Firefox 154 headless, `MOCK_API=false`.

- **PASS:** `loads verified bundle data without API mocks` (429ms)
- **PASS:** `passes the strict accessibility audit with live data` (379ms)

F-GOLD-05 dark muted token (`--raw-text-muted` 64% L) still holds; `color-contrast` still enabled. Thresholds were not weakened (`checkA11yStrict`).

`npm run test:a11y` → `a11y.cy.ts` `passes strict accessibility audit` (595ms).

---

## Failed checks

None.

---

## Blocked checks

None. Firefox was on PATH and used.

---

## Aggregate

`make gate-gold-release` **PASS** (exit 0). Log prints `✅ All gold release gates passed`.

**Verdict: PASS** for SHA `a607f244012bb8ca0faf81209cace38f73e8093d`.
