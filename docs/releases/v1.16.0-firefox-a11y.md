# ATB v1.16 Checkpoint C — Firefox / accessibility report

> Historical evidence for the candidate SHA(s) named below. This document does
> not certify the current candidate or authorise further implementation/release
> steps. Current status: [local product convergence](./v1.16.0-product-convergence.md).
> Original scratch filenames and local paths are preserved as historical provenance.

**Subagent E.** Product code was not edited during this proof. Thresholds were not weakened. Nothing was pushed or version-bumped.

**Branch:** `feat/v1.16.0-semantics`

| Run | SHA | Firefox | Accessibility |
| --- | --- | --- | --- |
| Initial Checkpoint C | `34b87a1bbc54d0795a5c3a744450526f14305a0a` | PASS | FAIL (focus restoration) |
| **F-A11Y-01 rerun (this document’s current verdict)** | `ea345fad651f7585693e35c4adc983433870e2de` | **PASS** | **PASS** |

Verified: `git rev-parse HEAD` = `ea345fad651f7585693e35c4adc983433870e2de` (required F-A11Y-01 HEAD; no further sibling commit). Chromium MCP was **not** used as Firefox proof.

Overall Checkpoint C is **not** marked PASS (live view / performance siblings may still be open).

---

## F-A11Y-01 rerun (ea345fa)

**Claim:** Escape on the command palette restores focus to the Commands trigger (`aria-label="Open command palette"`), not `BODY`.

**Commit:** `ea345fa fix(view): restore focus to Commands after palette Escape`  
Files: `web/app/view/components/CommandPalette.tsx`, `CommandPalette.test.tsx`. Vitest already PASS (not treated as Firefox proof).

**Since 34b87a1:**

```
ea345fa fix(view): restore focus to Commands after palette Escape
9c6bbcc fix(view): bind privacy reveal into inspector JSON
11a7b05 Revert "fix(view): bind privacy reveal into inspector JSON"
29cda1a fix(view): bind privacy reveal into inspector JSON
1208130 fix(view): stop showing omitted coverage as 0%
```

**How it was proven (same FAIL method as before):** Cypress Firefox 154 headless against live `atb view`. Open palette (Commands + native Enter) → trap Tab + ArrowDown/Up + `aria-activedescendant` → Escape → `document.activeElement` must be Commands, **not BODY**.

Embedded viewer was rebuilt (`cd web && npm run build`) then `go build -o /tmp/atb-v116-fa11y01 ./cmd/atb` so `atb view` served this SHA’s palette, not a stale embed.

| Item | Recorded |
| --- | --- |
| Firefox | `/Applications/Firefox.app/Contents/MacOS/firefox` **154.0.1** on PATH |
| Cypress | 15.16.0 `Browser: Firefox 154 (headless)` |
| Live View | `/tmp/atb-v116-fa11y01` on `http://127.0.0.1:18992` (quickstart bundle, session token as gold) |
| Real Firefox | `--headless --new-instance` PID 33830 against the same URL (running) |
| cursor-ide-browser | not used |

**Cypress result:** 1 passing, 0 failing.

```
F-A11Y-01 command palette focus restoration
  ✓ traps focus, moves aria-activedescendant, and restores Commands after Escape (1455ms)
```

Log: `/tmp/v116-fa11y01-cypress.log`. Spec copy: `/tmp/v116-fa11y01/v116-fa11y01-focus-restore.cy.ts` (not left in the product tree).

| Check | Result |
| --- | --- |
| Open palette | **PASS** |
| Focus trap (Tab combobox ↔ Close) | **PASS** |
| `aria-activedescendant` verify ↔ findings | **PASS** |
| ArrowUp / ArrowDown | **PASS** |
| Escape closes dialog | **PASS** |
| `document.activeElement` is Commands (`aria-label="Open command palette"`) | **PASS** |
| Not BODY | **PASS** |

**Accessibility verdict for F-A11Y-01 on ea345fa: PASS.**

---

## Initial run (34b87a1) — historical FAIL

That SHA failed live Firefox focus restoration: after Escape, `document.activeElement` was `<BODY>`. Gold axe / F-GOLD-05 contrast on 34b87a1 still cited; they did not prove keyboard restore. Full keyboard/overflow/zoom/narrow matrix from that run remains valid as Firefox-platform evidence and is not re-litigated here except F-A11Y-01.

Gold automation on 34b87a1 (not re-run for this F-A11Y-01 pass): `live-dashboard.cy.ts` strict a11y PASS; `a11y.cy.ts` PASS; F-GOLD-05 dark `--raw-text-muted` 64% L; `color-contrast` enabled.

---

## Failed checks

None on `ea345fad651f7585693e35c4adc983433870e2de`.

## Blocked checks

None for F-A11Y-01. (Page-level `cy.press('Shift+Tab')` remains a Cypress 15.16 harness limit from the initial matrix; not in F-A11Y-01 scope.)

---

## Verdict (current HEAD)

| Gate | Verdict |
| --- | --- |
| FIREFOX | **PASS** — Firefox 154.0.1 Cypress headless + real launch. |
| ACCESSIBILITY | **PASS** — F-A11Y-01 focus restoration holds in live Firefox; trap and activedescendant still work. |

Do not treat overall Checkpoint C as PASS.
