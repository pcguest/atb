# ATB v1.16 claims hygiene — Subagent H (hostile, read-only)

> Historical evidence for the candidate SHA(s) named below. This document does
> not certify the current candidate or authorise further implementation/release
> steps. Current status: [local product convergence](./v1.16.0-product-convergence.md).
> Original scratch filenames and local paths are preserved as historical provenance.

**Verdict (recertify after F-CLAIMS-01):** **PASS**  
**Candidate SHA:** `a607f244012bb8ca0faf81209cace38f73e8093d`  
**Branch:** `feat/v1.16.0-semantics`  
**HEAD:** matches required SHA.  
**Product edits this pass:** none.  
**Checkpoint E overall:** not marked PASS.

**Mortise vs this SHA:** Mortise boundary is **PASS WITH DATED COMPATIBILITY DEBT** on evidence SHA `ea345fad651f7585693e35c4adc983433870e2de` (`V116_MORTISE_CONFORMANCE.md`). F-CLAIMS-01 is docs/copy only (`Features.tsx`, `viewer.md`) and does not reopen Mortise product. Claims recertified on `a607f24`; Mortise evidence remains `ea345fa`.

Investigation FAIL on `ea345fa` stands as history. B1 and B2 copy closed by F-CLAIMS-01. Non-blocker STALE items remain accepted; this rerun did not FAIL on them.

---

## Recertification (this SHA)

Scope: previous blockers B1 and B2 only, plus a scan that F-CLAIMS-01 did not introduce a new public overclaim in those two files.

| ID | File | Required | Actual on `a607f24` | Result |
| --- | --- | --- | --- | --- |
| B1 | `web/components/Features.tsx` | Describe `.reveals` sidecar, not mutating the bundle | Masked Reveal Flow: “append an audit event to a `<bundle>.reveals` sidecar. The authoritative bundle is not mutated.” Diff is one sentence. | **CLOSED** |
| B2 | `docs/specification/viewer.md` | Must not present-tense ship Trust Dashboard / `SessionAnomalies` stats strip / 10k smoothness | Status: Incident-first investigation; no Health, Trust Dashboard, stats strip, Profile/CAS panel, or `SessionAnomalies` banner on `/view`. UI components rewritten to Incident…Trust. Performance: “does not claim 10k-event UI smoothness”; large-bundle smoothness is documented debt. | **CLOSED** |

### New overclaim scan (those two files only)

No new freeze FAIL.

- Features Local Viewer card still says “verification status, a timeline, a trace graph, and an event inspector”. **Unchanged** by F-CLAIMS-01. Already **MATERIAL STALE (non-blocker)**; not FAIL.
- `viewer.md` RoleSelector still “controls the role-aware panels”. **Unchanged**. Presentation-density only. Already **STALE (non-blocker)**; not FAIL.
- Profile/CAS remaining text is API endpoints (`GET /api/v1/bundle/profile`), explicitly “not a `/view` dashboard panel”. **CURRENT CORRECT**.
- Session list / anomaly badges remain under `/sessions` APIs, not shipped as `/view` Trust Dashboard chrome. **COMPATIBILITY / remaining spec**, not B2.
- New performance paragraph matches Subagent F (pagination 200 + load-more present; timeline not virtualised; graph unbounded; do not advertise 10k smoothness). **CURRENT CORRECT**.

Blockers remaining: **none**.

---

## Investigation FAIL (historical) — `ea345fa`

Authorised F-CLAIMS-01. Not re-opened.

### B1 (was FAIL) — Landing Features: privacy reveal mutates the bundle

Claimed “append an audit event **to the bundle**”. Actual: `<bundle>.reveals` sidecar. Market research had marked this P0 if landing still claimed bundle mutation.

### B2 (was FAIL) — Public viewer spec shipped retired Trust Dashboard on `/view`

Present-tense shipped: verification banner, timeline, trace graph, inspector, stats strip, Profile/CAS panel, `SessionAnomalies` banner; performance “handle 10k+ events smoothly”.

---

## Material STALE (non-blocker) — still accepted

Not FAIL on recertify.

| Location | Claim |
| --- | --- |
| `docs/getting-started/quickstart.md` | PyPI/npm `1.14.5` vs source `v1.15.2` |
| `web/README.md` | Trust Score widget / 5s polling as current dashboard readiness |
| `web/docs/api/viewer-contract.md` | Trust Dashboard shell |
| `web/components/CurrentScope.tsx` / `Features.tsx` Local Viewer | timeline / graph / inspector (incomplete vs Incident-first) |
| `web/app/workspace/page.tsx` | “immutable on disk” |
| `RoleSelector` | “Select dashboard role” / “role-aware panels” |
| `investigationSummary` | weaker empty-findings sentence than View empty-state |
| Cypress suite titles | “Trust Dashboard…” |

---

## Explicitly not FAIL

- MCP advertise-only `2024-11-05`
- `verify.report.v1.schema.3` current
- Shipped `/view` has no Health / Trust Dashboard / unbound “supplied to model”
- Versions still `1.15.4`
- View export CLI-only
- F-CAS-02 dated debt
- GROUP 7 `cli.py` / `--custos`

---

## Checkpoint E

Claims hygiene: **PASS** on `a607f24`.  
Mortise: **PASS WITH DATED COMPATIBILITY DEBT** on `ea345fa` (not rerun; docs-only F-CLAIMS-01 does not reopen Mortise product).  
**Overall Checkpoint E is not PASS.**
